{
    "name": "cypress-practical-test",
    "version": "1.0.0",
    "scripts": {
      "test": "cypress run",
      "cucumber": "cypress run --env TAGS='not @ignore'",
      "report": "npx mochawesome-merge cypress/reports/*.json > merged.json && npx mochawesome-report-generator merged.json"
    },
    
        "dependencies": {
          "@badeball/cypress-cucumber-preprocessor": "^22.0.1",
          "@bahmutov/cypress-esbuild-preprocessor": "^2.2.4",
          "cypress": "^14.2.1",
          "cypress-mochawesome-reporter": "^3.8.2"
        }
            
  }